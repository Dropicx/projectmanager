#!/usr/bin/env sh

echo "🔍 Running pre-commit checks..."

# Run lint-staged (formatting and linting)
pnpm pre-commit

# Run security audit (optional, continue on failure)
echo "🔒 Running security audit..."
pnpm security:audit || echo "⚠️  Security audit had warnings but continuing..."

# Type checking is optional - just show warnings
echo "📝 Running type checks (optional)..."
pnpm type-check 2>/dev/null || echo "⚠️  Type checking has errors - consider fixing them"

echo "✅ Pre-commit checks completed!"
