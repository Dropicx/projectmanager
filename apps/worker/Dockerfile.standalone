# Worker Dockerfile (for Railway with custom build command)
FROM node:22-alpine
WORKDIR /app

# Install dependencies
COPY apps/worker/package.json ./
RUN npm install --production

# Copy built files (assumes build happened during Railway build command)
COPY apps/worker/dist ./dist

CMD ["node", "dist/index.js"]